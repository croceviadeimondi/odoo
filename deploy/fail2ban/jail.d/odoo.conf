# /etc/fail2ban/jail.d/odoo.conf
#
# Jail fail2ban per il gestionale Odoo del Crocevia.
# Banna IP dopo 5 fallimenti di login in 10 minuti, per 1 ora.
#
# Per attivarlo:
#   sudo cp deploy/fail2ban/filter.d/odoo.conf  /etc/fail2ban/filter.d/
#   sudo cp deploy/fail2ban/jail.d/odoo.conf    /etc/fail2ban/jail.d/
#   sudo systemctl restart fail2ban
#   sudo fail2ban-client status odoo
#
# IMPORTANTE: Odoo deve loggare su file. In odoo.conf decommentare:
#   logfile = /var/log/odoo/odoo.log
# e montare il volume nel docker-compose:
#   - ./logs:/var/log/odoo
#
# ATTENZIONE al path: fail2ban gira sull'HOST, quindi `logpath` deve
# puntare al file sul lato HOST del mount ./logs, NON al path interno al
# container (/var/log/odoo/odoo.log). Sul VPS del Crocevia il repo e' in
# /opt/crocevia/gestionale, quindi il log host e':
#   /opt/crocevia/gestionale/logs/odoo.log

[odoo]
enabled  = true
port     = http,https
filter   = odoo
logpath  = /opt/crocevia/gestionale/logs/odoo.log
maxretry = 5
findtime = 600
bantime  = 3600
backend  = polling
